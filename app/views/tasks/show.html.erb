<p><%= @task.title %></p>
<p><%= @task.details %></p>
<%= link_to "Edit task", task_edit_path %>

<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
